# never commit keys, tokens, or transient scan artifacts
id_demo
id_demo.pub
*.pem
*token*
.env
bandit.json
host_results.json
__pycache__/
# NOTE: output/ (the generated register + ledger) IS committed by the pipeline

pipeline/ledger.jsonl
pipeline/register.xlsx
/ledger.jsonl
/register.xlsx
